interface:
  display_name: "cmux Release"
  short_description: "Draft release notes and run cmux release checks."
  default_prompt: "Use $cmux-release to draft Keep a Changelog release notes, bump versions, update CHANGELOG.md, run the pretag guard, tag, or troubleshoot release assets."
